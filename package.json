{
  "name": "trip-poor",
  "packageManager": "yarn@3.6.3",
  "private": true,
  "scripts": {
    "backend:build": "yarn workspace trip-poor-backend start",
    "backend": "yarn workspace trip-poor-backend start",
    "backend:dev": "yarn workspace trip-poor-backend start:dev"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "typescript": "^5.2.2"
  },
  "dependencies": {
    "@nestjs/config": "^3.0.1",
    "@nestjs/typeorm": "^10.0.0",
    "axios": "^1.5.0",
    "mysql2": "^3.6.0",
    "typeorm": "^0.3.17"
  }
}
